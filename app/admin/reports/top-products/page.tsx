import { getTopProducts } from "@/services/analytics.service";export const dynamic="force-dynamic";export default async function Page(){const rows=await getTopProducts(50);return <main className="container"><h1>Top 50 Products Daily</h1><div className="ops-table">{rows.map((r,i)=><article className="glass-card" style={{padding:"1rem"}} key={r.productId}><strong>#{i+1} {r.name}</strong><p className="small">Units {r.unitsSold} • Revenue ${r.revenue} • Repeat {r.repeatPurchases} • 7d {r.sevenDayTrend} • 30d {r.thirtyDayTrend}</p></article>)}</div></main>}
