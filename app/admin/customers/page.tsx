import { prisma } from "@/lib/prisma";import { safeQuery } from "@/lib/content-service";export const dynamic="force-dynamic";export default async function Page(){const customers=await safeQuery([],()=>prisma.user.findMany({include:{orders:true,profile:true},take:100}));return <main className="container"><h1>Customers</h1>{customers.map(c=><article className="glass-card" style={{padding:"1rem",marginBottom:"1rem"}} key={c.id}><h3>{c.email}</h3><p className="small">Role {c.profile?.role ?? "customer"} • orders {c.orders.length}</p></article>)}</main>}
