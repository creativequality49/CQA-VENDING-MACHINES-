import { isDatabaseConfigured, isSupabaseConfigured } from "@/lib/env";export default function Page(){return <main className="container"><h1>Settings</h1><article className="glass-card" style={{padding:"1rem"}}><p>Database: {isDatabaseConfigured()?"configured":"missing"}</p><p>Supabase Storage: {isSupabaseConfigured()?"configured":"missing"}</p><p className="small">Courier label provider can be connected by adding provider keys and extending fulfillment service.</p></article></main>}
