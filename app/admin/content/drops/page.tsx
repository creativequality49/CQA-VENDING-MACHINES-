import Link from "next/link";import { prisma } from "@/lib/prisma";import { safeQuery } from "@/lib/content-service";export const dynamic="force-dynamic";export default async function Page(){const drops=await safeQuery([],()=>prisma.contentDrop.findMany({include:{machine:true,product:true},orderBy:{scheduledDate:"asc"}}));return <main className="container"><h1>Scheduled Drops</h1><Link className="cta" href="/admin/content/drops/new">New Drop</Link><section className="grid grid-2">{drops.map(d=><article className="glass-card" style={{padding:"1rem"}} key={d.id}><h3>{d.dropTitle}</h3><p className="small">{d.status} • {d.scheduledDate?.toISOString() ?? "No schedule"}</p></article>)}</section></main>}
