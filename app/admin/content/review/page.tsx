import { prisma } from "@/lib/prisma";import { safeQuery } from "@/lib/content-service";export const dynamic="force-dynamic";export default async function Page(){const items=await safeQuery([],()=>prisma.contentItem.findMany({where:{status:"review"},orderBy:{createdAt:"asc"}}));return <main className="container"><h1>Review Queue</h1>{items.map(i=><article className="glass-card" style={{padding:"1rem",marginBottom:"1rem"}} key={i.id}><h3>{i.title}</h3><p className="small">{i.internalNotes}</p></article>)}</main>}
