import { notFound } from "next/navigation";import { prisma } from "@/lib/prisma";import { safeQuery } from "@/lib/content-service";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const item=await safeQuery(null,()=>prisma.contentItem.findUnique({where:{id},include:{machine:true,product:true}}));if(!item)notFound();return <main className="container"><article className="glass-card" style={{padding:"1rem"}}><h1>{item.title}</h1><p className="small">{item.status} • {item.accessType} • {item.contentType}</p><p>{item.description}</p><p className="small">File path: {item.filePath ?? "No file"}</p></article></main>}
