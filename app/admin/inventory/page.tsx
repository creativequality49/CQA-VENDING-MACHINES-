import { prisma } from "@/lib/prisma";import { safeQuery } from "@/lib/content-service";export const dynamic="force-dynamic";export default async function Page(){const [products,moves]=await Promise.all([safeQuery([],()=>prisma.product.findMany({orderBy:{stockQuantity:"asc"}})),safeQuery([],()=>prisma.inventoryMovement.findMany({include:{product:true},orderBy:{createdAt:"desc"},take:50}))]);return <main className="container"><h1>Inventory</h1><section className="grid grid-2">{products.map(p=><article className="glass-card" style={{padding:"1rem"}} key={p.id}><h3>{p.name ?? p.title}</h3><p className={p.stockQuantity<=p.lowStockThreshold?"warning":"small"}>Stock {p.stockQuantity} • Low threshold {p.lowStockThreshold}</p></article>)}</section><h2>Movement Log</h2>{moves.map(m=><p className="small" key={m.id}>{m.createdAt.toISOString()} • {m.product.title} • {m.movementType} • {m.quantityChange}</p>)}</main>}
