import { getCurrentUser } from "@/lib/auth";import { prisma } from "@/lib/prisma";import { safeQuery } from "@/lib/content-service";export const dynamic="force-dynamic";export default async function Page(){const user=await getCurrentUser();const access=user?await safeQuery([],()=>prisma.digitalAccess.findMany({where:{customerId:user.id,accessStatus:"ACTIVE"},include:{product:true},orderBy:{createdAt:"desc"}})):[];return <main className="container"><h1>Downloads</h1>{access.map(a=><article className="glass-card" style={{padding:"1rem",marginBottom:"1rem"}} key={a.id}><h3>{a.product.title}</h3><p className="small">Downloads {a.downloadCount} • last {a.lastDownloadedAt?.toISOString() ?? "never"}</p></article>)}</main>}
